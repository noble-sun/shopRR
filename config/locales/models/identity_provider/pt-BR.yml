pt-BR:
  activerecord:
    models:
      identity_provider:
        one: Provedor de Identidade
        other: Provedores de Identidade
    attributes:
      access_token: Token de acesso
      account_identitfiers: Idendificador da conta
      expires_in: Expira em
      id_token: id token
      name: Nome
      refresh_token: Token de atualização
    errors:
      models:
        identity_provider:
          attributes:

